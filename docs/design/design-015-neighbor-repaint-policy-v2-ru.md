# Политика пересчета соседних тайлов при покраске (v2)

Статус: Implemented
Дата: 2026-02-23
Ревизия: v2 (переработка после аудита совместимости с ADR-0011 и plan-015)

## Связанные документы

- ADR: [ADR-0011-autotile-routing-architecture.md](../adr/ADR-0011-autotile-routing-architecture.md)
- Design Doc: [design-015-autotile-routing-system.md](design-015-autotile-routing-system.md)
- Work Plan: [plan-015-autotile-routing-system.md](../plans/plan-015-autotile-routing-system.md)

---

## 1. Цель

Этот документ задает детальный алгоритм:

- когда после покраски клетки нужно **фактически обновлять** результат пересчета соседних клеток;
- когда пересчет соседа технически выполняется, но его результат **не изменяет** видимый вывод;
- как классифицировать ребра между материалами для предсказуемого визуального результата.

Документ является **дополнением** к основному Design Doc (design-015) и ADR-0011. Он не заменяет их, а уточняет поведение внутри существующей архитектуры RetileEngine.

### 1.1. Принцип совместимости

Этот документ **не меняет** следующие архитектурные решения:

| Решение | Источник | Статус |
|---------|----------|--------|
| Chebyshev R=2 dirty propagation | ADR-0011 Decision 2, Design Doc AC6 | Сохраняется без изменений |
| RetileEngine.expandDirtySet (до 25 клеток на покраску) | retile-engine.ts | Сохраняется без изменений |
| Reverse-pair fallback (ADR Decision 7) | ADR-0011 Decision 7 | Полноценный механизм |
| S1/S2 conflict resolution | ADR-0011 Decision 6 | Работает на всех ребрах |
| EdgeResolver (3 варианта: both-valid / one-valid / neither-valid) | edge-resolver.ts | Сохраняется без изменений |
| Порог 50% для full-pass | retile-engine.ts expandDirtySet | Сохраняется без изменений |

---

## 2. Ключевая идея

### 2.1. Два уровня: зона пересчета и визуальная стабильность

Архитектура RetileEngine оперирует **зоной кандидатов на пересчет** (dirty set) -- множеством клеток, для которых движок пересчитывает тайлсет, маску и фрейм. Эта зона определяется **геометрически** (Chebyshev R=2) и не подлежит сужению.

Политика, описанная в этом документе, работает на **другом уровне**. Она не фильтрует dirty set, а отвечает на вопрос: **для каких пересчитанных клеток результат будет визуально осмысленным?** Это знание полезно для:

1. **Понимания поведения**: разработчик может предсказать, какие клетки реально изменят свой визуал после покраски.
2. **Диагностики**: если сосед не обновился визуально, это может быть ожидаемым поведением, а не багом.
3. **Будущей оптимизации**: если в будущем потребуется уменьшить dirty set для производительности, классификация ребер подскажет, какие клетки можно безопасно исключить.
4. **Тестирования**: для какой пары ожидать визуальное изменение соседа, а для какой нет.

### 2.2. Общий принцип

Все клетки в dirty set (R=2 Chebyshev) технически пересчитываются **и коммитятся безусловно**. Compute-фаза детерминирована: одинаковый полный вход compute-фазы дает одинаковый результат. Commit-фаза записывает результат для **каждой** клетки dirty set без исключений.

Классификация ребер C1-C5 (раздел 4) **не влияет на commit**. Она используется для:

1. **Понимания поведения**: предсказание, какие клетки реально изменят визуал после покраски.
2. **Диагностики**: если сосед не обновился визуально, классификация объясняет, почему frame остался прежним.
3. **Будущей оптимизации**: если потребуется сузить dirty set, классификация подскажет безопасные кандидаты на исключение.
4. **Тестирования**: какие пары ожидаемо стабильны, а какие нет.

Важно: commit каждой клетки не гарантирует смену номера `frame` -- после пересчета кадр может совпасть с предыдущим.

---

## 3. Термины

1. **Центр** -- клетка, материал которой пользователь только что изменил. В batch-триггерах (`fill/rectangle`) центр трактуется как множество измененных клеток (`centers`).
2. **Сосед (кардинальный)** -- в рамках классификации это кардинальный сосед центра (N/E/S/W). Для него напрямую применяется классификация C1-C5.
3. **Сосед (диагональный)** -- не классифицируется как отдельное ребро. Фиксируется только если обе прилегающие к нему кардинали стабильны (`C1/C2/C3`).
4. **Dirty set** -- множество клеток, пересчитываемых RetileEngine после покраски. Определяется геометрически: Chebyshev R=2 вокруг каждой покрашенной клетки. При >50% карты -- full-pass.
5. **Прямая пара** (direct pair) -- существует тайлсет `A_B` (`fromMaterialKey=A, toMaterialKey=B`).
6. **Обратная пара** (reverse pair) -- прямой пары `A_B` нет, но есть `B_A`. Используется с обратной ориентацией маски. Является **полноценным** механизмом по ADR-0011 Decision 7.
7. **Маршрут** (route) -- результат `nextHop(A, B)` из BFS routing table. Может возвращать промежуточный материал-мост M или null (нет пути). Таблица строится по `compatGraph` (совместимость недиректед): ребро `A <-> B` существует, если в наборе есть хотя бы один переход `A_B` или `B_A`.
8. **Владелец ребра** (edge owner) -- результат `resolveEdge()`: сторона границы, которая определяет **какой тайлсет/BG** используется для перехода на этом ребре. Владение влияет на **выбор тайлсета**, а не на вычисление битов маски. Используется в S1/S2 conflict resolution.
9. **Resolved pair** -- результат `registry.resolvePair(fg, bg)`: возвращает `{ tilesetKey, orientation: 'direct' | 'reverse' }` или null.

### 3.1. Краткий глоссарий (быстрый lookup)

| Термин | Что это простыми словами |
|---|---|
| `compute-phase` | Пересчет `tileset/frame` для всех клеток dirty set |
| `commit-phase` | Безусловная запись результата для всех клеток dirty set в visual/cache |
| `owner-side` | Правило: владелец ребра определяет **какой тайлсет/BG** выбирается для перехода. Не влияет на вычисление битов маски |
| `bridge` (`M`) | Промежуточный материал маршрута между двумя FG (пример: `deep_water -> water -> grass`) |
| `base-mode` | Режим без transition-bg (`bg=''`). Два подрежима: (a) если есть хотя бы один foreign кардинал -- frame = SOLID_FRAME(1) напрямую; (b) если все кардинали same-FG/OOB -- FG-equality mask -> FRAME_TABLE |
| `transition-mode` | Режим с выбранным `bg != ''`. Маска считается по FG-equality (bit=1 если сосед same-FG или OOB), затем gating, затем FRAME_TABLE |
| `ring-solid override` | Спец-правило только для isolated C2: `center=47`, первый пояс принудительно `1` |
| ~~`mandatoryCommitSet`~~ | *(Концептуальный термин, не реализован.)* Предполагаемый набор клеток обязательного commit; в текущей реализации все dirty-клетки коммитятся безусловно |
| ~~`optionalCommitSet`~~ | *(Концептуальный термин, не реализован.)* Предполагаемый набор клеток необязательного commit; в текущей реализации не используется |

---

## 4. Классификация ребер

### 4.1. Зачем нужна классификация

EdgeResolver (edge-resolver.ts) имеет три исхода: both-valid / one-valid / neither-valid. Классификация ребер **расширяет** этот набор дополнительной информацией о **качестве стыка**, что позволяет предсказать визуальный результат.

Сама классификация не меняет расчеты маршрутизации/маски. Она используется для предсказания визуального качества стыка и является основой для будущих оптимизаций (см. [§13.3](#133-будущая-оптимизация-selective-commit)).

### 4.2. Исходные данные для классификации ребра A <-> B

Для каждого кардинального ребра между материалами A и B вычисляются:

```
hopA = nextHop(A, B)     -- маршрут A в сторону B
hopB = nextHop(B, A)     -- маршрут B в сторону A
pairA = resolvePair(A, hopA)  -- тайлсет для A-стороны (direct/reverse/null)
pairB = resolvePair(B, hopB)  -- тайлсет для B-стороны (direct/reverse/null)
```

Контракт router для этой классификации: `nextHop` работает по графу совместимости (а не только по direct-ребрам render-графа), поэтому reverse-пара участвует в достижимости пути.

### 4.3. Пять классов ребер

| Класс | Условие | Визуальная стабильность | Пример |
|-------|---------|------------------------|--------|
| **C1: Одноматериальный** | A === B | Всегда стабилен | grass <-> grass |
| **C2: Симметричный прямой** | pairA.orientation === 'direct' И pairB.orientation === 'direct' И hopA === B И hopB === A | Высокая | grass <-> water (оба направления существуют как прямые тайлсеты) |
| **C3: Устойчивый с мостом** | pairA.orientation === 'direct' И pairB.orientation === 'direct' И hopA === hopB И hopA !== A И hopA !== B | Высокая | deep_water <-> grass через мост water (оба прямые) |
| **C4: Смешанный валидный** | pairA !== null И pairB !== null, но не C2/C3 (хотя бы одна сторона reverse, или мосты разные) | Средняя/низкая | deep_water <-> water (direct + reverse) |
| **C5: Частичный или невалидный** | hopA === null ИЛИ hopB === null ИЛИ pairA === null ИЛИ pairB === null | Низкая | Гипотетическая несвязная пара |

### 4.4. Связь с EdgeResolver

| EdgeResolver result | Возможные классы ребра |
|--------------------|----------------------|
| both-valid | C2, C3, C4 |
| one-valid | C5 |
| neither-valid | C5 |
| null (same material) | C1 |

Классификация ребер -- это надстройка над EdgeResolver. Она используется для предсказания визуального качества стыка:

1. C1/C2/C3 -> визуально предсказуемый результат; оба соседа обычно получают корректные фреймы.
2. C4/C5 -> визуально менее предсказуемый результат; сосед может получить корректный фрейм, но это не гарантируется классификацией.
3. Диагонали визуально зависят от двух прилегающих кардиналей (гейтинг); стабильность диагонали определяется стабильностью обеих опорных кардиналей.

### 4.5. Глоссарий классов C1-C5 (простыми словами)

| Класс | Что означает | Визуальная предсказуемость |
|---|---|---|
| `C1` | Оба материала одинаковые (`A === B`) | Всегда корректный стык |
| `C2` | Оба направления прямые и симметричные (`A_B` и `B_A`) | Высокая; isolated-center ring-solid допустим |
| `C3` | Обе стороны прямые через один и тот же мост `M` | Высокая; первый пояс считается по коннекторам |
| `C4` | Обе стороны валидны, но есть reverse или мосты разные | Средняя/низкая; визуал зависит от ориентации |
| `C5` | Хотя бы у одной стороны нет валидного пути/пары | Низкая; возможен базовый fallback |

---

## 5. Общий алгоритм пересчета (пошагово)

Алгоритм ниже описывает поведение **существующего** RetileEngine с учетом классификации ребер.

### Шаг 1. Формирование dirty set

При покраске одной или нескольких клеток RetileEngine вызывает `expandDirtySet()`:

- Для каждой покрашенной клетки (x, y) добавляются все клетки в квадрате [x-2..x+2, y-2..y+2] (Chebyshev R=2, до 25 клеток на покрашенную клетку).
- Если общий размер dirty set превышает 50% карты, переключается на full-pass (все клетки карты).
- В batch-режиме (fill, rectangle): dirty set = объединение R=2 расширений всех покрашенных клеток.

**Это не изменяется данной политикой.**

### Шаг 2. Обновление grid

Материалы покрашенных клеток обновляются в копии grid (immutability).

### Шаг 3. Пересчет каждой клетки в dirty set

Для **каждой** клетки в dirty set (без исключений) RetileEngine выполняет полный цикл:

1. Вычисление virtualBG для каждого кардинального ребра через `nextHop`.
2. Проверка доступности тайлсетов через `resolvePair` (direct-first, reverse fallback).
3. Выбор логического тайлсета: `selectTilesetForCell(fg, ownedEdges, registry)`.
4. Conflict resolution (S1/S2) при множественных BG.
5. Вычисление маски (base-mode или transition-mode).
6. Диагональный гейтинг.
7. Lookup фрейма через `getFrame`.
8. Frame-source resolution (renderTilesetKey).

### 5.1. Алгоритмические детали маски (8-bit -> blob-47)

Чтобы исключить неоднозначность, маска и фрейм считаются в строгом порядке. Порядок битов: `N=1, NE=2, E=4, SE=8, S=16, SW=32, W=64, NW=128`.

#### 5.1.1. FG-equality mask (единый для всех режимов)

Для каждого из 8 направлений вычисляется бит по правилу **FG-equality**:
- `bit = 1`, если сосед по этому направлению имеет тот же FG-материал, что и текущая клетка;
- `bit = 1`, если сосед за пределами карты (OOB);
- `bit = 0`, если сосед имеет другой FG-материал.

Результат -- `rawMask` (8 бит). Это **единственный** способ построения маски; owner-side контракт **не** влияет на значения битов.

#### 5.1.2. Три режима вычисления frame

После построения `rawMask` выбирается один из трех режимов:

**Режим A: transition-mode** (`bg != ''`, клетка владеет хотя бы одним foreign кардинальным ребром)

1. Применяется диагональный гейтинг к `rawMask` -> `gatedMask`.
2. `frame = FRAME_TABLE[gatedMask]`.

*Пример*: grass(2,2) в 5x5 truth. FG=grass, bg=water. Кардиналы: N=water(0), E=deep_water(0), S=deep_water(0), W=deep_water(0). rawMask = 0. Gating: все кардинали 0 -> все диагонали gated -> gatedMask=0. FRAME_TABLE[0] = 47 (ISOLATED_FRAME).

**Режим B: base-mode с foreign кардиналами** (`bg == ''`, хотя бы один кардинальный сосед имеет другой FG)

1. `frame = SOLID_FRAME(1)` напрямую, без FRAME_TABLE.

*Пример*: water(2,1) в 5x5 truth. FG=water, bg=''. Кардиналы: N=deep_water(0), E=deep_water(0), S=grass(0), W=deep_water(0). Есть foreign кардинал -> frame=1.

*Пример*: water(1,1) в 3x3 water-in-deep_water. FG=water, bg=''. Все 4 кардинала = deep_water (foreign). -> frame=1.

**Режим C: base-mode без foreign кардиналов** (`bg == ''`, все кардинальные соседи same-FG или OOB)

1. Применяется диагональный гейтинг к `rawMask` -> `gatedMask`.
2. `frame = FRAME_TABLE[gatedMask]`.

*Пример*: deep_water(0,0) в 5x5. FG=deep_water, bg=''. Кардиналы: N=OOB(1), E=deep_water(1), S=deep_water(1), W=OOB(1). Все кардинали = 1 (same-FG/OOB). rawMask = 255. Gating: все кардинали 1 -> все диагонали сохранены. gatedMask=255. FRAME_TABLE[255] = 1 (SOLID_FRAME).

#### 5.1.3. Диагональный гейтинг

Диагонали проходят обязательный гейтинг (в режимах A и C):
- `NW` может быть 1 только если `N=1` и `W=1`;
- `NE` может быть 1 только если `N=1` и `E=1`;
- `SE` может быть 1 только если `S=1` и `E=1`;
- `SW` может быть 1 только если `S=1` и `W=1`.

Это соответствует инварианту из `design-015`: диагональ никогда не может "открыться сама по себе" без двух опорных кардиналей.

#### 5.1.4. Reverse orientation

Для orientation `reverse`: `frame = getFrame((~mask47Input) & 0xFF)`.
`getFrame()` выполняет `gateDiagonals()` внутри, поэтому после инверсии диагональный гейтинг применяется повторно на инвертированной маске.

#### 5.1.5. Роль owner-side контракта

Owner-side контракт (`resolveEdge()`, S1/S2) определяет **какой BG/тайлсет** выбирается для клетки, а не какие биты маски устанавливаются. Конкретно:
- Если клетка владеет ребром к соседу -> это ребро участвует в выборе BG через `selectTilesetForCell`.
- Если клетка не владеет ни одним foreign ребром -> `bg = ''` (base-mode).
- Биты маски всегда вычисляются по FG-equality независимо от владения.

### Шаг 4. Запись результата

Все клетки dirty set коммитятся **безусловно**. Пересчитанный результат (tileset, frame, cache) записывается для каждой клетки без фильтрации по классу ребра.

Специальные правила:

1. **Ring-solid override (только C2)**: Для симметричной двухматериальной C2-пары (`A_B` и `B_A`, без моста): если центр получил `frame=47` (ISOLATED_FRAME), все 8 клеток первого кольца вокруг центра принудительно получают `frame=1` (SOLID_FRAME).
2. **C3 bridge -- без ring-solid**: Для мостовых C3-пар (пример: `deep_water <-> grass` через `water`) первый пояс не получает принудительный `frame=1`: кадры каждой клетки считаются по маске/коннекторам. Для центра frame получается из transition-маски; например, для острова FG `1x1` результатом будет `frame=47`, а для кластера -- connected-кадры.

Итог записывается в `layers[].frames` и `layers[].tilesetKeys` (immutable output), кеш обновляется для всех клеток dirty set.

> **Замечание о будущей оптимизации**: Концептуально можно разделить dirty set на `mandatoryCommitSet` (центр, стабильные кардинали C1/C2/C3, стабильные диагонали) и `optionalCommitSet` (остальные). Это описано в [§13.3](#133-будущая-оптимизация-selective-commit) и не реализовано в текущей версии.

### Шаг 5. Возврат RetileResult

`RetileResult` содержит обновленные layers и patches для undo.

### 5.2. Почему пересчет локальный, но шире чем 1-радиус

В учебных примерах autotile обычно пересчитывают измененную клетку и ближайших соседей. В текущей архитектуре локальность сохранена, но зафиксирована как `Chebyshev R=2`:

1. Это все еще локальный пересчет, а не full-map rebuild.
2. Радиус 2 -- это консервативный инженерный контракт для единого поведения одиночной и batch-покраски, без ветвления логики по инструментам.
3. Для одиночной покраски R=2 не является строгой математической зависимостью маски клеток дистанции 1; это выбранный запас локальности для детерминизма и уменьшения визуальных скачков в сложных локальных конфигурациях.
4. Поэтому R=2 трактуется как policy/engineering-контракт (single/batch consistency), а не как обязательная топологическая зависимость маски ближайших соседей.

---

## 6. Визуальное поведение по классам ребер

Классификация ребер используется для предсказания визуального результата после пересчета. Все клетки dirty set коммитятся безусловно; классификация описывает **ожидаемое качество стыка**, а не правила фильтрации commit.

### 6.1. C1: Одноматериальный (A === B)

Оба соседа одного материала. Пересчет гарантированно дает корректный результат: маски и фреймы сшиваются.

**Ожидание**: Оба соседа пересчитываются и коммитятся; их `frame` может остаться прежним; швов не возникает.

### 6.2. C2: Симметричный прямой

Обе стороны имеют прямые тайлсеты. Это наиболее устойчивый переход.

**Ожидание**: Обе клетки на границе пересчитываются и коммитятся. Owner-side определяет выбор BG/тайлсета, а не биты маски. Для isolated-center ring-solid используется правило из [§5, Шаг 4 (п.1)](#шаг-4-запись-результата).

**Пример**: `grass <-> water`. Существуют `grass_water` и `water_grass`.

### 6.3. C3: Устойчивый с мостом

Обе стороны валидны прямым способом через один и тот же промежуточный материал.

**Ожидание**: Обе клетки пересчитываются и коммитятся. Owner-side определяет выбор BG/тайлсета. Общий мост гарантирует совместимость transitionType.

Механика расчета transition для C3 через общий мост `M`:

1. Фрейм клетки определяется связностью собственной FG-области (same-FG кардинали).
2. Внешняя граница к материалу-партнеру учитывается как стык через мост `M`, а не как принудительный ring-solid.
3. `frame` вычисляется из этой маски без отдельного спец-`if` для `1x1`.
4. Для `1x1` и `2x2+` используются те же общие правила маски/коннекторов (без отдельных исключений для C3).

**Пример**: `deep_water <-> grass` через мост `water`. `nextHop(deep_water, grass) = water`, тайлсет `deep-water_water` (direct). `nextHop(grass, deep_water) = water`, тайлсет `grass_water` (direct).

### 6.4. C4: Смешанный валидный (reverse или разные мосты)

Обе стороны имеют resolved pair, но хотя бы одна сторона использует reverse orientation или мосты не совпадают.

**Ожидание**:

1. Все клетки (центр и соседи) пересчитываются и коммитятся безусловно.
2. Визуальная предсказуемость ниже, чем у C2/C3: результат корректен, но менее интуитивен для пользователя.

Причины пониженной предсказуемости:

- Reverse orientation может давать визуально отличающийся угловой рисунок (инверсная маска).
- Разные мосты означают, что стороны опираются на разные transition-type ожидания.
- Owner-side определяет BG/тайлсет, что при reverse может привести к неочевидным визуальным комбинациям.

**Пример**: `deep_water <-> water`. `nextHop(deep_water, water) = water`, тайлсет `deep-water_water` (direct). `nextHop(water, deep_water) = deep_water`, тайлсет `deep-water_water` (reverse, т.к. `water_deep-water` не существует как прямой тайлсет).

**Важно**: reverse-pair остается полноценным механизмом для вычисления перехода центра, но сам по себе не переводит ребро в устойчивый класс C2/C3.

### 6.5. C5: Частичный или невалидный

Хотя бы у одной стороны нет resolved pair (resolvePair вернул null).

**Ожидание**: Сторона без resolved pair не сможет отобразить переход. Она получит базовый тайлсет (no-transition mode, FG-equality mask). Визуально это выглядит как "жесткий обрыв" между материалами.

EdgeResolver вернет one-valid (если одна сторона рабочая) или null (если обе нерабочие). В обоих случаях класс ребра считается C5 с точки зрения визуальной предсказуемости.

---

## 7. Взаимодействие с S1/S2 conflict resolution

### 7.1. S1 (Owner Reassign) работает на всех классах, но локально для клетки

S1 применяется внутри `CellTilesetSelector` только для текущей пересчитываемой клетки, когда у нее несколько конкурирующих BG. В текущей реализации S1:

- сокращает набор `ownedEdges` этой клетки по приоритетам;
- определяет, **какой BG/тайлсет** выбирается для этой клетки;
- не записывает глобально новый owner-контракт ребра соседу.

S1 влияет на **локальный выбор тайлсета**, а не на вычисление маски или состав commit-набора. Commit безусловный для всех клеток dirty set.

Это работает **независимо от класса ребра**:

- Для C2/C3: S1 локально выбирает устойчивого BG-кандидата; визуальный результат предсказуем.
- Для C4: S1 локально снимает конфликт; визуальный результат менее предсказуем из-за reverse/разных мостов.
- Для C5: S1 может оставить единственный рабочий локальный вариант либо привести к fallback.

**S1 и commit -- разные уровни.** S1 влияет на локальный выбор BG/тайлсета клетки. Commit безусловно записывает результат для всех dirty-клеток.

### 7.2. S2 (BG Priority Fallback) не зависит от классификации

S2 выбирает BG с наивысшим приоритетом при неразрешенном конфликте. Это чисто детерминистический fallback, не учитывающий класс ребра.

---

## 8. Специальные условия (без сценариев)

### 8.1. Диагональные соседи

Диагональные клетки попадают в dirty set (R=2 включает их). Они пересчитываются и коммитятся безусловно, как и все клетки dirty set:

- Диагональные ребра не участвуют в EdgeResolver (только кардинальные N/E/S/W).
- Диагонали влияют на угловые биты маски через `gateDiagonals()`.
- Визуальная предсказуемость диагонали определяется стабильностью обеих прилегающих кардиналей (C1/C2/C3).
- C2 isolated-center override для диагоналей определяется общим правилом [§5, Шаг 4 (п.1)](#шаг-4-запись-результата).
- Клетки на расстоянии 2 пересчитываются как часть консервативного R=2 контракта. Это не строгая зависимость маски distance=1, а выбор для единого и детерминированного поведения в single и batch сценариях.

См. примеры: [12.1](#example-12-1), [12.5](#example-12-5), [12.6](#example-12-6).

### 8.2. Выход за границы карты

Вне карты сосед не существует. `expandDirtySet` ограничивает координаты `[0, width-1]` и `[0, height-1]`. OOB-направления для масок: `bit = 1` (matching).

См. пример: [12.8](#example-12-8).

### 8.3. Перекраска в тот же материал

Если `newFG === oldFG`, RetileEngine все равно формирует dirty set и пересчитывает. Результат обычно идентичен предыдущему (детерминизм), но может отличаться, если окружение изменилось с момента последнего пересчета.

См. примеры: [12.1](#example-12-1), [12.2](#example-12-2) (повторная покраска тем же FG как no-op).

### 8.4. Цепочка покрасок

Каждая покраска -- независимое событие. Dirty set формируется заново. Клетка, которая не обновила визуал при предыдущей покраске (неустойчивый стык), может обновиться при следующей покраске, если попадет в новый dirty set и ее контекст изменится.

См. примеры: [12.5](#example-12-5) (расширение острова до `2x2+`), [12.6](#example-12-6) (смена контекста ребер).

### 8.5. Batch-режим (fill, rectangle)

Dirty set = объединение R=2 расширений всех покрашенных клеток. Все клетки пересчитываются. Порог 50% может сработать для крупных fill-операций, переключая на full-pass.
В batch-режиме `centers` = множество всех покрашенных клеток; все клетки объединенного dirty set коммитятся безусловно.

См. пример: [12.7](#example-12-7).

---

## 9. Правила детерминизма

1. **Порядок обработки**: RetileEngine обрабатывает dirty set в порядке flat index (top-to-bottom, left-to-right). S1 итерации используют тот же порядок.
2. **Direct-first**: При наличии обоих вариантов (direct и reverse) всегда выбирается direct (ADR Decision 7).
3. **Priority tie-break**: При равных приоритетах в EdgeResolver выбирается лексикографически меньший materialKey.
4. **Preference tie-break**: При равных BFS-путях Router использует preference array.
5. **Immutability**: Входной state никогда не мутируется. Результат -- новый объект.
6. **Полный вход детерминизма**: детерминизм определяется по полному входному состоянию: `grid + layers + cache + triggerType + options/materials/tilesets/priorities/preferences`.
7. **Нет истории-зависимости**: все dirty-клетки коммитятся безусловно, поэтому результат не зависит от предыдущего visual/cache state. Одинаковый полный вход дает одинаковый результат.
8. **Paint == rebuild**: результат серии paint-операций эквивалентен full rebuild при одинаковом финальном `grid`, потому что commit безусловный для всех dirty-клеток.

---

## 10. Проверка на текущем наборе данных (`/api/editor-data`)

### 10.1. Параметры набора

Дата: 2026-02-23. Материалов: 10. Уникальных пар: C(10,2) = 45.

### 10.2. Классификация всех 45 пар

Для классификации каждой пары (A, B) нужны:
- `hopA = nextHop(A, B)`, `hopB = nextHop(B, A)`
- `pairA = resolvePair(A, hopA)`, `pairB = resolvePair(B, hopB)`

#### C2: Симметричный прямой (hop = прямой сосед, оба direct)

1. `grass <-> water` -- прямые тайлсеты `grass_water` и `water_grass`

#### C3: Устойчивый с мостом (оба direct, общий мост)

2. `deep_sand <-> deep_water` -- мост `water` (оба direct: `sand_water`, `deep-water_water`)
3. `deep_water <-> grass` -- мост `water` (оба direct: `deep-water_water`, `grass_water`)
4. `dusty_soil <-> fertile_soil` -- мост `grass` (оба direct)
5. `dusty_soil <-> sand` -- мост `grass`
6. `dusty_soil <-> soil` -- мост `grass`
7. `dusty_soil <-> tilled_soil` -- мост `grass`
8. `dusty_soil <-> water` -- мост `grass`
9. `dusty_soil <-> watered_soil` -- мост `grass`
10. `fertile_soil <-> sand` -- мост `grass`
11. `fertile_soil <-> soil` -- мост `grass`
12. `fertile_soil <-> tilled_soil` -- мост `grass`
13. `fertile_soil <-> water` -- мост `grass`
14. `fertile_soil <-> watered_soil` -- мост `grass`
15. `sand <-> soil` -- мост `grass`
16. `sand <-> tilled_soil` -- мост `grass`
17. `sand <-> watered_soil` -- мост `grass`
18. `soil <-> tilled_soil` -- мост `grass`
19. `soil <-> water` -- мост `grass`
20. `soil <-> watered_soil` -- мост `grass`
21. `tilled_soil <-> water` -- мост `grass`
22. `tilled_soil <-> watered_soil` -- мост `grass`
23. `water <-> watered_soil` -- мост `grass`

#### C4: Смешанный валидный (reverse или разные мосты)

Примечание: Пары ниже имеют resolved pair для обеих сторон, но не удовлетворяют условиям C2/C3.
Для центра они валидны, но для соседа не считаются обязательным триггером фиксации изменения.

24. `deep_sand <-> dusty_soil`
25. `deep_sand <-> fertile_soil`
26. `deep_sand <-> grass`
27. `deep_sand <-> sand`
28. `deep_sand <-> soil`
29. `deep_sand <-> tilled_soil`
30. `deep_sand <-> water`
31. `deep_sand <-> watered_soil`
32. `deep_water <-> dusty_soil`
33. `deep_water <-> fertile_soil`
34. `deep_water <-> sand`
35. `deep_water <-> soil`
36. `deep_water <-> tilled_soil`
37. `deep_water <-> water`
38. `deep_water <-> watered_soil`
39. `dusty_soil <-> grass`
40. `fertile_soil <-> grass`
41. `grass <-> sand`
42. `grass <-> soil`
43. `grass <-> tilled_soil`
44. `grass <-> watered_soil`
45. `sand <-> water`

#### C5: Частичный или невалидный

На текущем наборе данных: **0 пар**. Все 45 пар имеют resolved pair для обеих сторон.

### 10.3. Статистика

| Класс | Количество | Доля |
|-------|-----------|------|
| C2 (симметричный прямой) | 1 | 2.2% |
| C3 (устойчивый с мостом) | 22 | 48.9% |
| C4 (смешанный валидный) | 22 | 48.9% |
| C5 (частичный/невалидный) | 0 | 0% |
| **Всего** | **45** | **100%** |

### 10.4. Замечание о классификации

Точная классификация каждой пары зависит от конкретных тайлсетов в наборе данных. Числа выше могут измениться если:

- Добавятся новые тайлсеты (reverse-пары станут direct, C4 перейдет в C2/C3).
- Изменится routing table (другие мосты, другая классификация).

Рекомендуется пересчитывать классификацию при каждом изменении набора тайлсетов. Для этого можно использовать утилиту:

```typescript
function classifyEdge(
  a: string, b: string,
  router: RoutingTable,
  registry: TilesetRegistry,
): 'C1' | 'C2' | 'C3' | 'C4' | 'C5' {
  if (a === b) return 'C1';

  const hopA = router.nextHop(a, b);
  const hopB = router.nextHop(b, a);

  const pairA = hopA !== null ? registry.resolvePair(a, hopA) : null;
  const pairB = hopB !== null ? registry.resolvePair(b, hopB) : null;

  if (pairA === null || pairB === null || hopA === null || hopB === null) return 'C5';

  const bothDirect = pairA.orientation === 'direct' && pairB.orientation === 'direct';

  if (!bothDirect) return 'C4';

  // Both direct. Check bridge conditions.
  if (hopA === b && hopB === a) return 'C2'; // Direct neighbors, no bridge

  const sameBridgeIntermediate =
    hopA === hopB &&
    hopA !== a &&
    hopA !== b;

  if (sameBridgeIntermediate) return 'C3'; // Same intermediate bridge M
  return 'C4'; // Different bridges or non-intermediate match
}
```

---

## 11. Замечание о данных

Для перехода `tilled_soil -> grass` используется ключ `tilled-soil_grass`. Этот документ и тестовые фикстуры синхронизированы с этим именем.

---

## 12. Практические примеры

Ниже приведены детальные сценарии для ручной проверки и для формулировки ожидаемого поведения в тестах.

#### Example 12-1
### 12.1. `water` поле, одиночный `grass` в центре (C2 isolated-center)

1. Setup:
   - вся область заполнена `water`;
   - до покраски все клетки обычно `water_grass` с `frame=1`.
2. Action:
   - покрасить одну центральную клетку в `grass`.
3. Классификация:
   - ребра `grass <-> water` относятся к C2 (оба направления direct).
4. Что коммитятся:
   - все клетки dirty set коммитятся безусловно.
5. Ожидание:
   - центр использует `grass_water`;
   - в isolated-center включается override из [§5, Шаг 4 (п.1)](#шаг-4-запись-результата): `center=47`, весь первый пояс `frame=1`;
   - клетки дистанции 2 пересчитаны, но обычно визуально не меняются.

#### Example 12-2
### 12.2. `water` поле, одиночный `deep_water` в центре (C4)

1. Setup:
   - вся область `water`.
2. Action:
   - покрасить центр в `deep_water`.
3. Классификация:
   - `deep_water <-> water` относится к C4 (valid pair на обеих сторонах, но не C2/C3).
4. Что коммитятся:
   - все клетки dirty set коммитятся безусловно.
5. Ожидание:
   - центр получает transition-тайлсет `deep-water_water`;
   - соседние `water`-клетки пересчитываются, но их визуал может не измениться (C4 -- пониженная визуальная предсказуемость).

#### Example 12-3
### 12.3. `water` поле, одиночный `deep_sand` в центре (C4)

1. Setup:
   - вся область `water`.
2. Action:
   - покрасить центр в `deep_sand`.
3. Классификация:
   - `deep_sand <-> water` относится к C4.
4. Что коммитятся:
   - все клетки dirty set коммитятся безусловно.
5. Ожидание:
   - центр получает валидный переход через `water` (тайлсет стороны `deep_sand`);
   - соседние клетки пересчитываются; визуальный результат менее предсказуем из-за C4.

#### Example 12-4
### 12.4. `deep_water` поле, одиночный `deep_sand` в центре (C3 через мост `water`)

1. Setup:
   - вся область `deep_water`.
2. Action:
   - покрасить центр в `deep_sand`.
3. Классификация:
   - `deep_sand <-> deep_water` относится к C3 (общий мост `water`, обе стороны direct).
4. Что коммитятся:
   - все клетки dirty set коммитятся безусловно.
5. Ожидание:
   - переход устойчивый с обеих сторон через общий мост (C3 -- высокая визуальная предсказуемость);
   - первый пояс считается по коннекторам (без C2 ring-solid override).

#### Example 12-5
### 12.5. `water` поле + кольцо `grass` + центр `deep_water` (C3 bridge island)

1. Setup:
   - поле `water`;
   - вокруг центра построено кольцо `grass` (3x3 без центра).
2. Action:
   - покрасить центр в `deep_water`.
3. Классификация:
   - ключевой стык `deep_water <-> grass` относится к C3 через мост `water`.
4. Что коммитятся:
   - все клетки dirty set коммитятся безусловно.
5. Ожидание:
   - центр выбирает `deep-water_water`;
   - первый пояс (`grass` и прилегающие клетки) считается по коннекторам, не принудительно в `frame=1`;
   - для острова `1x1` центр обычно получает `frame=47`;
   - при расширении `deep_water` до кластера `2x2+` центр перестает быть изолированным и получает connected-кадры.

#### Example 12-6
### 12.6. Смешанное окружение центра (комбинация C1/C2/C3/C4)

1. Setup:
   - центр `soil`;
   - соседи: `N=water`, `E=grass`, `S=deep_water`, `W=sand`.
2. Action:
   - покрасить/перекрасить центр в целевой FG.
3. Классификация:
   - каждое из 4 кардинальных ребер центра получает свой класс C1-C5.
4. Что коммитятся:
   - все клетки dirty set коммитятся безусловно.
5. Ожидание:
   - единый BG для центра выбирается через S1/S2;
   - визуальная предсказуемость каждого ребра определяется его классом (C1/C2/C3 -- высокая, C4 -- средняя/низкая);
   - маска для каждой клетки вычисляется по FG-equality (см. [§5.1](#51-алгоритмические-детали-маски-8-bit---blob-47)).

#### Example 12-7
### 12.7. Batch-покраска прямоугольника `grass` в поле `water`

1. Setup:
   - поле заполнено `water`;
   - инструмент: прямоугольник/fill, одновременно красим несколько клеток в `grass`.
2. Action:
   - выполнить batch-покраску блока, например `3x3`.
3. Классификация:
   - внутренние стыки `grass <-> grass` относятся к C1;
   - внешняя граница блока к `water` относится к C2.
4. Что коммитятся:
   - все клетки объединенного dirty set коммитятся безусловно;
   - при крупном объеме может включиться full-pass по порогу 50%.
5. Ожидание:
   - единый детерминированный результат независимо от порядка обхода внутри батча;
   - на внешнем контуре ребра C2 дают предсказуемый визуальный стык;
   - маска каждой клетки вычисляется по FG-equality.

#### Example 12-8
### 12.8. Покраска у границы карты (OOB)

1. Setup:
   - выбрать клетку на границе (например, `x=0` или `y=0`) в однородном поле.
2. Action:
   - покрасить граничную клетку в другой материал.
3. Классификация:
   - для существующих соседей действуют обычные C1-C5;
   - для отсутствующих соседей вне карты используется OOB-правило маски.
4. Что коммитятся:
   - все клетки dirty set коммитятся безусловно;
   - dirty set обрезается по границам карты.
5. Ожидание:
   - нет выхода за пределы массива и нет артефактов от несуществующих соседей;
   - OOB-направления трактуются как matching (`bit=1`) при расчете маски.

---

## 13. Интеграция с RetileEngine

### 13.1. Точка интеграции

В текущей реализации RetileEngine работает с безусловным commit:

1. `expandDirtySet()` формирует dirty set (Chebyshev R=2).
2. `rebuildCells()` вычисляет tileset/frame для всех клеток dirty set.
3. Результат записывается (commit) для **всех** клеток dirty set безусловно.
4. Cache и visual state обновляются для всех пересчитанных клеток.
5. S1/S2 влияют на локальный выбор BG/тайлсета клетки, но не на состав commit-набора (он = весь dirty set).

Это обеспечивает полную детерминированность: одинаковый вход всегда дает одинаковый результат, без зависимости от предыдущего visual/cache state.

### 13.2. Будущая оптимизация (не для текущей реализации)

Если в будущем потребуется уменьшить dirty set для производительности, классификация может использоваться как **фильтр второго уровня**:

```
candidateDirtySet = expandDirtySet(changedCells)  // R=2 Chebyshev
filteredDirtySet = candidateDirtySet  // default: пересчитать все

// Опциональная оптимизация (не реализована):
// for each cell in candidateDirtySet:
//   if cell is NOT a painted cell:
//     if ALL edges to changed neighbors are C5:
//       skip cell (no visual change expected)
```

Такая оптимизация безопасна для C5 и требует отдельного профилирования для C4.
Для текущей версии обязательна только политика фиксации результата, без изменения геометрии dirty set.

### 13.3. Будущая оптимизация: selective commit (концепция)

В будущем, если потребуется уменьшить количество записываемых клеток (например, для сетевой передачи delta-патчей), можно реализовать selective commit на основе классификации ребер:

- `mandatoryCommitSet = union_per_center( center ∪ stableCardinals(C1/C2/C3) ∪ stableDiagonals(по правилу угла) )`.
- `optionalCommitSet = dirtySet \ mandatoryCommitSet`.
- Для `optionalCommitSet` запись может быть отложена, если клетка уже имеет валидный visual/cache state.

Правило "устойчивого угла" для диагоналей: диагональ входит в mandatory commit, только если обе прилегающие кардинали стабильны (C1/C2/C3):
- NW: стабильны N и W.
- NE: стабильны N и E.
- SE: стабильны S и E.
- SW: стабильны S и W.

**Статус**: концепция, не реализована. Текущая реализация коммитит все dirty-клетки безусловно.

---

## 14. Минимальный набор регрессионных проверок

Ниже -- тесты, которые валидируют корректность визуального поведения с учетом классификации ребер. Они **дополняют** (не заменяют) тесты из plan-015 Phase 8.

### 14.1. Проверки по классам

1. **C2**: Покраска `grass` в поле `water`. Ожидание: все dirty-клетки коммитятся; центр получает `grass_water` с `frame=47` (FG-equality mask: все кардинали foreign -> mask=0 -> ISOLATED_FRAME); ring-solid override: весь первый пояс `frame=1`.
2. **C3**: Покраска `deep_sand` в поле `deep_water`. Ожидание: все dirty-клетки коммитятся; кардинальные соседи получают предсказуемый визуал через общий мост `water`; конкретный `frame` может совпасть с предыдущим, но шов остается устойчивым.
3. **C4**: Покраска `deep_water` в поле `water`. Ожидание: все dirty-клетки коммитятся; визуальный результат соседей менее предсказуем (C4).
4. **C4**: Покраска `deep_sand` в поле `water`. Ожидание: все dirty-клетки коммитятся; вода вокруг может визуально не измениться (frame остается прежним).
5. **Owner-side контракт**: для каждого кардинального ребра `A != B` проверяется, что owner-side определяет **выбор BG/тайлсета**, а не биты маски. Маска всегда вычисляется по FG-equality.
6. **Диагональный гейтинг**: кейсы с одиночной диагональю (`NE=1` при `N=0` или `E=0`) не должны сохранять диагональный бит после `gateDiagonals`.
7. **Reverse-ориентация**: при отсутствии direct-пары и наличии reverse-пары фрейм берется через `getFrame((~mask47Input) & 0xFF)` после `gateDiagonals(rawMask)`, а не через прямой lookup.
8. **Детерминизм**: повторный запуск тех же операций дает идентичный результат (тайлсеты, фреймы, маски).
9. **Batch**: Fill 100 клеток `grass` в поле `water`. Все клетки в объединенном R=2 dirty set пересчитаны.
10. **Диагональная предсказуемость**: единичная покраска центра в окружении, где стабильны (C1/C2/C3) только N и E. Ожидание: NE-диагональ визуально предсказуема; NW/SE/SW визуально менее предсказуемы. Все коммитятся безусловно.
11. **C3 bridge island**: `water` поле + кольцо `grass` + центр `deep_water`. Ожидание: первый пояс и диагонали считаются по коннекторам (не принудительно `1`), а в частном случае острова `1x1` transition-маска дает центру `frame=47`.

### 14.2. Регрессионные проверки (из design-015 AC9)

12. **P1**: deep-water fill + soil rectangle -- корректные deep-water_water переходы.
13. **P2**: soil square + water lake -- корректные water_grass blob-47 corners.
14. **P3**: deep-water | soil | sand strips -- маршрутизация через мосты.
